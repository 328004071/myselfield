git add filename
git commit filename -s "description"


github:
#make public key
ssh-keygen -t rsa   

#copy id_rsa_pub to github's SSH and GPG keys

#初始化github
git init

#提交缓存区
git add .

#提交至本地仓库
git commit -m "备注信息"

#查询
git log


